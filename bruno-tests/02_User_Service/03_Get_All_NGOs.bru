meta {
  name: Get All NGOs
  type: http
  seq: 3
}

get {
  url: {{user_service_url}}/api/v1/users/ngos?page=0&size=10
  body: none
  auth: inherit
}

params:query {
  page: 0
  size: 10
}

tests {
  test("Should retrieve NGOs", function() {
    expect(res.status).to.equal(200);
    expect(res.body).to.be.an("array");
  });
}