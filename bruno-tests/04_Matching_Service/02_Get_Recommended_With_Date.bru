meta {
  name: Get Recommended Postings with Date
  type: http
  seq: 2
}

get {
  url: {{matching_service_url}}/api/v1/matching/recommend/{{volunteer_id}}?location=Greenville&domain=Environment&date=2024-12-20
  body: none
  auth: inherit
}

params:query {
  location: Greenville
  domain: Environment
  date: 2024-12-20
}

tests {
  test("Should get recommended postings with date filter", function() {
    expect(res.status).to.equal(200);
    expect(res.body).to.be.an("array");
  });
}