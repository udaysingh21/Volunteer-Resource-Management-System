meta {
  name: Register via Matching Service
  type: http
  seq: 3
}

post {
  url: {{matching_service_url}}/api/v1/matching/register/{{volunteer_id}}/{{posting_id}}
  body: none
  auth: inherit
}

tests {
  test("Should register volunteer via matching service", function() {
    expect(res.status).to.equal(200);
    expect(res.body).to.have.property("message");
    expect(res.body).to.have.property("volunteerId");
    expect(res.body).to.have.property("postingId");
  });
}