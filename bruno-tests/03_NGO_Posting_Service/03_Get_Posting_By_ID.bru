meta {
  name: Get Posting By ID
  type: http
  seq: 3
}

get {
  url: {{ngo_service_url}}/api/v1/postings/{{posting_id}}
  body: none
  auth: inherit
}

tests {
  test("Should retrieve posting by ID", function() {
    expect(res.status).to.equal(200);
    expect(res.body).to.have.property("id");
    expect(res.body).to.have.property("title");
    expect(res.body).to.have.property("domain");
  });
}