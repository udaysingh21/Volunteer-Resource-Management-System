meta {
  name: Get Postings By Domain
  type: http
  seq: 5
}

get {
  url: {{ngo_service_url}}/api/v1/postings/domain/Environment?page=0&size=20
  body: none
  auth: inherit
}

params:query {
  page: 0
  size: 20
}

tests {
  test("Should retrieve postings by domain", function() {
    expect(res.status).to.equal(200);
    expect(res.body).to.have.property("content");
    expect(res.body.content).to.be.an("array");
  });
}