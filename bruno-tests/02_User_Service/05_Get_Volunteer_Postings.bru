meta {
  name: Get Volunteer Postings
  type: http
  seq: 5
}

get {
  url: {{user_service_url}}/api/v1/users/volunteers/{{volunteer_id}}/postings
  body: none
  auth: inherit
}

tests {
  test("Should retrieve volunteer's postings", function() {
    expect(res.status).to.equal(200);
    expect(res.body).to.be.an("array");
  });
}