meta {
  name: Register Volunteer for Posting
  type: http
  seq: 4
}

post {
  url: {{user_service_url}}/api/v1/users/volunteers/{{volunteer_id}}/postings/{{posting_id}}
  body: none
  auth: inherit
}

tests {
  test("Should register volunteer for posting", function() {
    expect(res.status).to.equal(200);
    expect(res.body).to.have.property("message");
  });
}